<UserControl x:Class="CalcBuddy.Calculator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:forms="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"        
             xmlns:local="clr-namespace:CalcBuddy"
             mc:Ignorable="d" 
             x:Name="parent"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Grid.Row="0">
			<ToggleButton Content="Plot" IsChecked="{Binding ElementName=parent,Path=IsPlotAreaVisible}" Focusable="False" />
		</StackPanel>

		<forms:WindowsFormsHost x:Name="host" Grid.Row="1">
			<local:ConsoleTextBox x:Name="consoleTextBox" WordWrap="True" />
		</forms:WindowsFormsHost>
	</Grid>
</UserControl>
